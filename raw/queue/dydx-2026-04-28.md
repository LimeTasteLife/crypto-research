# dYdX v4 ingest queue — 2026-04-28
#
# Per AGENTS.md §"Batch ingest workflow": each non-comment URL becomes a
# separate snapshot (root + path-prefix). Crawler auto-discovers children
# within same host + same path prefix. Comments / blank lines ignored.
#
# === Phase 0 recon notes (2026-04-28) ===
#
# Three distinct GitBook properties identified:
#   1. docs.dydx.xyz       — integration / API / concepts / governance (canonical user-facing)
#   2. docs.dydx.community/dydx                  — governance module deep-dives + parameter tables
#   3. docs.dydx.community/dydx-chain-technical-docs — chain ops, validators, FE deployment
#
# Aliasing: docs.dydx.exchange/ returns IDENTICAL HTML to docs.dydx.xyz/ as of
# 2026-04-28. Treated as legacy alias — DO NOT crawl separately to avoid
# duplicate mirror snapshots. (queue-001.md still lists it; should be retired.)
#
# Out of scope for mirror layer (handle separately if needed):
#   - github.com/dydxprotocol/v4-chain  (source code, not docs format)
#   - www.dydx.xyz/blog/*  (marketing/operator posts; high noise, fetch ad-hoc)
#
# === Frame coverage expectations (perpdex 9 angles × 5 patterns) ===
#
# Roots above are predicted to cover:
#   A1 launch       — community/dydx (Markets, LiquidityTiers) + xyz/concepts/trading/governance
#   A2 fee-model    — xyz/concepts + community/dydx (FeeTiers module)
#   A3 referral     — TWO distinct mechanisms (PARTIAL coverage prediction):
#                     (1) Order Router Rev Share — xyz/interaction/integration/integration-revshare
#                         (PPM share to routers, governance-voted, references proposal 311)
#                     (2) Affiliate Program — community/dydx/dydx-features/affiliate-program
#                         (Standard tier ≤15% taker fee comm, $3k USDC/30d cap;
#                          VIP ≤50%, $10k/referral/30d cap; $10k vol gate;
#                          on-chain USDC payout; managed by CryptoLearningClub via dYdX Grants)
#   A4 MM           — community/dydx (FeeTiers maker rebate, -1.1bps initial setting)
#                     — gap risk: no first-party MM-agreement disclosure expected
#   A5 spot-LP      — N/A for v4 (no spot AMM); expect "not-applicable" rationale in coverage
#   A6 risk         — xyz/concepts/trading/{liquidations,margin} + community/dydx (LiquidationsConfig)
#   A7 governance   — community/dydx (full module surface) + xyz/concepts/trading/governance
#   A8 security     — community/dydx-chain-technical-docs (validator slashing, halts, upgrades)
#   A9 regulatory   — gap risk: not first-party documented; will surface as `gap` cell
#
# === Predicted gaps to validate post-ingest ===
#
# G(dydx)-1 — Per-market parameter time-series (A1 × P-IV stack-accounting):
#   Expectation: governance proposal JSON specifies one snapshot only;
#   historical changes only reconstructable via on-chain proposal log.
#   Mirror crawl will NOT capture this. Flag in audit postmortem.
#
# G(dydx)-2 — Order Router Rev Share recipients (A3 × P-V SLA-contracting):
#   integration-revshare doc explains mechanism + governance vote requirement,
#   but list of currently-voted-in addresses lives on-chain (proposal 311 ref'd).
#   Off-chain doc gap — same shape as HL Gap 2 (programme outcome KPI).
#
# G(dydx)-3 — MM agreement / market-maker program (A4 × P-V):
#   Public docs state -1.1bps maker rebate as protocol-level setting;
#   no operator-facing MM agreement template/SLA found in recon.
#   May not exist (v4 is permissionless) — flag as "not-applicable" candidate.
#
# G(dydx)-4 — Affiliate Program operator metadata (A3 × P-V SLA-contracting):
#   Standard/VIP tier criteria + $10k vol gate + 30-day caps documented in
#   first-party doc (community/dydx/dydx-features/affiliate-program). What is
#   NOT first-party documented: list of approved affiliates, performance
#   metrics per affiliate, sybil/wash controls, and the operational arrangement
#   with CryptoLearningClub (compensation, SLA, scope). Same shape as
#   G(dydx)-2 — mechanism documented, operator-side detail off-chain only.

https://docs.dydx.xyz/
https://docs.dydx.community/dydx
https://docs.dydx.community/dydx-chain-technical-docs
