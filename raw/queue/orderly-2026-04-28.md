# Orderly Network ingest queue — 2026-04-28
#
# Per AGENTS.md §"Batch ingest workflow": each non-comment URL becomes a
# separate snapshot (root + path-prefix). Crawler auto-discovers children
# within same host + same path prefix. Comments / blank lines ignored.
#
# === Phase 0 recon notes (2026-04-28) ===
#
# Two distinct doc properties identified (one canonical + one SDK reference):
#   1. orderly.network/docs/            — canonical builder + trader docs
#                                         (Mintlify). Covers: architecture,
#                                         API auth, accounts, deposit/withdrawal,
#                                         order lifecycle, trading concepts
#                                         (fees, funding, margin, liquidation,
#                                         mark price), permissionless listing,
#                                         OmniVault, tokenomics, security.
#                                         llms.txt index confirmed available at
#                                         https://orderly.network/docs/llms.txt
#                                         — use for BFS seeding (Mintlify nav is
#                                         client-side rendered).
#   2. orderly.network/listing/         — public listing application page
#                                         (listing criteria, referral bonus
#                                         programme for referrers, oracle
#                                         requirements, liquidity depth specs).
#                                         Distinct content; not part of /docs/
#                                         path prefix. Single-page or shallow.
#                                         Treat with +nochildren or bounded
#                                         crawl; marketing landing with
#                                         actionable parameter data (depth
#                                         thresholds, oracle list, referral
#                                         bonus formula) not duplicated in /docs/.
#
# Aliasing detected (3 aliases confirmed — DO NOT crawl separately):
#   docs.orderly.network/           — fetched 2026-04-28; returns IDENTICAL HTML
#                                     to orderly.network/docs/home (same Mintlify
#                                     build, same navigation, same llms.txt
#                                     Documentation Index header). Legacy CNAME
#                                     alias. Excluded from flat URL list.
#   staging-docs.orderly.network/   — fetched 2026-04-28; returns IDENTICAL HTML
#                                     content to orderly.network/docs/home. Differs
#                                     only in feedback API endpoint (uses
#                                     "orderly-network-staging" project ID vs
#                                     "orderly"). This is a preview/staging deploy
#                                     of the same docs repo. DO NOT crawl; would
#                                     produce duplicate mirror content.
#   docs.orderly.network/docs       — implicit sub-path of the alias above;
#                                     excluded by the alias drop.
#
# Out of scope for mirror layer (handle separately if needed):
#   - github.com/OrderlyNetwork/documentation-public  (MDX source for docs;
#     mirrored via orderly.network/docs/)
#   - github.com/OrderlyNetwork/js-sdk                (SDK source code)
#   - orderly.network/blog/*                          (marketing/ecosystem posts;
#     high noise — fetch ad-hoc; blog on DWF market-maker partnership and
#     permissionless listing launch are relevant for A4/A1 claims but not
#     systematic crawl targets)
#   - api.orderly.org/ + testnet-api.orderly.org      (runtime API endpoints,
#     not doc pages)
#   - dex.orderly.network/                            (Orderly One live DEX;
#     app, not docs)
#
# Orderly-specific structural notes for crawl agents:
#   - orderly.network/docs/ uses Mintlify renderer with client-side nav.
#     Probe https://orderly.network/docs/llms.txt to seed BFS — confirmed
#     available and serves a clean page-URL index (50+ pages enumerated).
#   - SDK tech-doc pages (orderly.network/docs/sdks/tech-doc/...) contain
#     auto-generated TypeScript interface docs (e.g. SymbolExt with
#     cap_funding, floor_funding, funding_period, liquidator_fee fields).
#     These are high-value for A1/A6 parameter extraction but large in volume;
#     apply +max-pages=150 to accommodate.
#   - orderly.network/listing/ is a marketing page with structured criteria
#     data. Use +nochildren to avoid crawling app/blog links that may be
#     reachable from it.
#   - Orderly is incubated by WOO Network; multi-sig governance (not DAO
#     token-voting yet). Governance is "being formalized" per tokenomics docs
#     — expect A7 coverage to be thin in first-party docs.
#
# === Frame coverage expectations (perpdex 9 angles × 5 patterns) ===
#
# Orderly subtype determination: PERPDEX (confirmed). Orderly operates a shared
# CLOB orderbook infrastructure; builders deploy frontends on top; all perp
# markets settle through the same Orderly matching engine. The entity is the
# infrastructure layer + its liquidity pool, not the individual builder DEXs.
# Builder DEXs may eventually warrant separate entries, but for now Orderly
# Network is filed as perpdex with the "multi-frontend" builder model noted.
#
# Roots above are predicted to cover:
#
#   A1 launch       — orderly.network/docs/introduction/trade-on-orderly/
#                     permissionless-listing (permissionless listing: Diamond
#                     Tier gate $10B/30d vol or 7M staked $ORDER; $50k/market
#                     IF requirement; POST_ONLY → ACTIVE phased launch via
#                     ±2% depth $100k × 10min; oracle sources: Pyth + Stork +
#                     CEX feeds; per-Builder insurance isolation; isolated
#                     margin only for community-listed markets);
#                     orderly.network/listing/ (standard listing criteria:
#                     $20M+ market cap, 3+ price oracles, 20k depth -/+2%,
#                     15 levels, ≤50bps spread, ≥90% uptime LP).
#                     Coverage: STRONG on permissionless listing mechanics and
#                     standard listing criteria. Listing governance authority
#                     (who approves standard listings at Orderly level?) —
#                     PARTIAL; likely multi-sig-gated but not documented in
#                     public docs.
#
#   A2 fee-model    — orderly.network/docs/introduction/trade-on-orderly/
#                     trading-basics/trading-fees (two-layer fee: Orderly base
#                     fee charged to Builder + user fee set by Builder;
#                     Base taker: Crypto 3.00→1.00bps by tier, RWA 5.00→3.00bps;
#                     Base maker: 0.00bps across all tiers; Builder keeps margin);
#                     Builder Staking Programme tiers determine base fee;
#                     permissionless-listing creators earn 50% of trading fees
#                     + 100% of liquidation fees from their markets.
#                     Coverage: STRONG on fee structure. Revenue distribution
#                     between Orderly protocol and builders — documented.
#                     Buyback cadence for $ORDER (from 30% net fee daily
#                     treasury buyback) — PARTIAL (mechanism described in
#                     tokenomics, exact cadence/TWAP parameters unclear).
#
#   A3 referral     — orderly.network/listing/ (listing referral bonus:
#                     $2,000 fixed + 30% of project marketing budget variable,
#                     capped $10k total per referral; applies to project
#                     referrers bringing tokens to list, NOT trader referrals).
#                     No trader referral program surface found in recon.
#                     Coverage: PARTIAL — listing referral documented;
#                     trader-level referral program existence unconfirmed from
#                     recon; may surface post-ingest in docs.
#
#   A4 MM           — orderly.network/docs/introduction/orderly-omniVault/
#                     overview + mechanics (OmniVault: omnichain strategy vault
#                     managed by Kronos Research as Strategy Provider; deploys
#                     market-making strategies on Orderly orderbook; earns
#                     yield from MM profits + liquidation position takeovers;
#                     48-hour lock-up; PnL proportionally distributed to LPs;
#                     omnichain USDC deposit from Arbitrum/Base/Optimism/Sei;
#                     two OmniVault audits completed per docs);
#                     permissionless listing Builder MM accounts (per-Builder
#                     MM sub-accounts for community markets).
#                     Coverage: STRONG on OmniVault/protocol-MM surface.
#                     Toxic-flow / markout-adjusted rebate for MMs — PARTIAL
#                     (no markout disclosure in recon). MM concentration HHI
#                     — GAP (likely requires on-chain query).
#
#   A5 spot-LP      — OmniVault is the primary LP/vault surface (A4 overlaps
#                     significantly). Orderly is perp-only at the protocol
#                     level (no spot DEX AMM pairs). OmniVault LPs are
#                     effectively MM-strategy depositors, not traditional
#                     spot-AMM LPs. A5 frame applicability:
#                     PARTIALLY APPLICABLE — OmniVault has LP deposit/
#                     withdrawal lifecycle, lock-up, yield mechanics, but
#                     these are strategy vault LP, not AMM LP. Cells A5/PI
#                     through A5/PV may need "not-applicable — perp-only venue;
#                     OmniVault is strategy vault LP, filed under A4" rationale
#                     OR filed as A5 with "vault LP" subtype note.
#                     Flag in frame coverage decision for Phase 2.
#
#   A6 risk         — orderly.network/docs/introduction/trade-on-orderly/
#                     perpetual-futures/insurance-fund-and-adl (per-Builder IF
#                     isolation for community-listed markets; shared platform IF
#                     for standard markets; ADL triggered when IF margin ratio
#                     drops below min_insurance_fund_margin_ratio AND no
#                     liquidators; ADL scope = single market only for
#                     community-listed);
#                     funding-rate (clamp formula: Cap/Floor per-market;
#                     Interest Rate fixed 0.01%; 1h/4h/8h periods per market;
#                     Premium component from spread snapshot);
#                     margin/liquidation docs expected in /docs/introduction/
#                     trade-on-orderly/perpetual-futures/* subtree.
#                     Coverage: STRONG on IF topology, ADL, funding mechanics.
#                     IF coverage ratio as a numeric (A6 × PI) — GAP:
#                     formula documented; live balance on-chain only.
#
#   A7 governance   — orderly.network/docs/introduction/security (multi-sig
#                     approver mechanism; signers with "deepest interest in
#                     success"; governance "being formalized" per tokenomics.
#                     $ORDER staking grants voting rights "as governance is
#                     formalized" — i.e., token governance not yet active).
#                     Coverage: WEAK — formal governance docs thin; multi-sig
#                     operational control with no DAO voting yet. Parameter-
#                     change authority and timelock absent from first-party
#                     docs found in recon. Flag A7 as GAP-heavy.
#
#   A8 security     — orderly.network/docs/introduction/security (auditors:
#                     Halborn, Zellic, Guardian Audits, + independent
#                     consultants; reports on GitHub; OmniVault has 2 audits;
#                     incubated by WOO Network);
#                     WOO Network relationship implies shared security context.
#                     Bug bounty existence — NOT found in recon; may be absent
#                     or only in GitHub SECURITY.md.
#                     Coverage: PARTIAL — audit list exists but sparse details
#                     in first-party docs. Bug bounty program — UNCONFIRMED.
#
#   A9 regulatory   — Not found in first-party docs; Orderly is non-custodial
#                     infrastructure layer; "multi-frontend" model means
#                     regulatory exposure is partly delegated to individual
#                     builder DEXs. No jurisdiction block list or legal opinion
#                     page found in recon.
#                     Coverage: GAP — will surface as gap cell.
#
# === Predicted gaps to validate post-ingest ===
#
# G(orderly)-1 — Per-market parameter time-series (A1 × P-IV stack-accounting):
#   Expectation: SDK tech-doc (SymbolExt interface) exposes current params
#   (cap_funding, floor_funding, base_imr, base_mmr, liquidator_fee,
#   funding_period) per symbol as a live snapshot. Historical changes to
#   these per-market params are not published in static docs; requires
#   on-chain or API query. Same shape as G(dydx)-1 and G(drift)-1.
#
# G(orderly)-2 — Standard listing governance authority (A1 × P-II onboarding):
#   Who approves standard listings at the Orderly protocol level? Docs show
#   Orderly evaluates and approves (vs. permissionless self-listing by
#   Diamond-Tier builders), but the specific committee, multi-sig composition,
#   or governance process for standard listing approval is not documented.
#   Expected to be an internal Orderly team decision with multi-sig execution.
#
# G(orderly)-3 — Trader referral program existence and mechanics (A3 × P-I/PII):
#   Only listing-referral bonus found in recon (project teams referring new
#   assets to list). Standard trader referral program (referrer earns % of
#   referred trader's fees) — existence unconfirmed. May not exist or may be
#   builder-discretionary (each DEX implements their own referral). Gap in
#   first-party docs; validate post-ingest.
#
# G(orderly)-4 — OmniVault LP exit mechanics under stress (A4/A5 × P-II, P-I):
#   OmniVault has 48-hour lock-up and withdrawal claim lifecycle documented at
#   high level (deposit → period start → withdrawal request → 6h processing →
#   claim). Stressed-state exit behavior (if strategy is at a loss, vault
#   pause conditions, what happens to in-flight withdrawals during adverse PnL)
#   is NOT documented in recon. Same shape as G(drift)-6.
#
# G(orderly)-5 — MM agreement / SLA for Builder-deployed MM accounts (A4 × P-V):
#   Permissionless listing Builders bind optional MM accounts. No formal MM
#   agreement or quote-obligation SLA template found in recon. Likely
#   not-applicable (Builder takes full responsibility, no external MM contract
#   template exists), but must validate post-ingest.
#
# G(orderly)-6 — Bug bounty program (A8 × P-V SLA-contracting):
#   Audit list documented (Halborn, Zellic, Guardian Audits). Bug bounty
#   program existence — NOT found in recon. May exist only in GitHub
#   SECURITY.md (out of mirror scope). Flag as unconfirmed; validate.
#
# G(orderly)-7 — Regulatory classification / jurisdiction block list (A9 × P-I–V):
#   No first-party legal opinion, blocked-jurisdiction list, or KYC tier doc
#   found. Orderly is permissionless infrastructure; frontends are builder-
#   owned. A9 likely maps to GAP for Orderly itself (builders own the
#   jurisdictional surface). Confirm post-ingest.
#
# G(orderly)-8 — $ORDER governance formalization timeline (A7 × P-II):
#   Docs state governance "is being formalized" with $ORDER staking granting
#   future voting rights. No concrete proposal, timeline, or governance forum
#   link found in recon. Gap: A7 coverage will be thin until governance is
#   live; expected to remain GAP post-ingest.
#
# === Crawler instructions for Phase 0.5 (out-of-band) ===
#
# Path-prefix containment: orderly.network/docs/ (root #1 below) may
# technically subsume any /docs/introduction/, /docs/build-on-omnichain/,
# /docs/sdks/ etc. subtrees, but since all content lives under a single
# Mintlify deployment at the same host+prefix, ONE snapshot is correct —
# do NOT split into sub-prefix snapshots.
#
# IMPORTANT: Use llms.txt to seed BFS. The root page at
# orderly.network/docs/home renders client-side; the llms.txt index at
# orderly.network/docs/llms.txt provides a clean list of all in-scope URLs.
# Feed this to the BFS queue after the seed fetch.
#
# For orderly.network/listing/ (root #2): Use +nochildren to capture only
# the listing criteria page itself. The page contains structured criteria
# data (oracle list, depth thresholds, referral bonus formula) not duplicated
# in /docs/. Avoid following links to the app, blog, or partner sites.

https://orderly.network/docs/
https://orderly.network/listing/ +nochildren
