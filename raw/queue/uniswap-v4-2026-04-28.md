# Uniswap v4 ingest queue — 2026-04-28
#
# Per AGENTS.md §"Batch ingest workflow": each non-comment URL becomes a
# separate snapshot (root + path-prefix). Crawler auto-discovers children
# within same host + same path prefix. Comments / blank lines ignored.
#
# *** SUBTYPE DECISION — READ FIRST ***
#
# Uniswap v4 is NOT a perpdex. It is a DEX substrate (AMM protocol with
# hooks-based extensibility). The perpdex frame (A1-A9 × P-I to P-V)
# applies ONLY TRANSITIVELY: if/when a 3rd-party perpetual futures hook
# is deployed on Uniswap v4 pools, THAT hook entity would be the perpdex
# and would receive a standard perpdex ingest. Uniswap v4 itself is the
# substrate those perpdex hooks run on.
#
# Proposed entity subtype: `dex-substrate` (new, to be proposed via
# open-questions.md). Closest existing subtype is `network` but that
# implies a blockchain, which v4 is not. `dex-substrate` would capture:
# Uniswap v4, Uniswap v3 (as a future candidate), Balancer v2 pools,
# Curve, etc. — DEX protocol layers that perpdex hooks or vaults build on.
# Until the subtype is accepted, file as `perpdex` with `subtype_note`
# clarifying substrate role, OR hold as a stub with an open-question.
#
# RECOMMENDATION: File Uniswap v4 as a new `network`-adjacent stub with
# a clearly-marked `## Subtype note` explaining substrate classification.
# Do NOT force-fit as perpdex. Raise `dex-substrate` as schema extension
# in open-questions.md during Phase 2.
#
# === Phase 0 recon notes (2026-04-28) ===
#
# One distinct doc property identified (single Mintlify deployment, two hostnames):
#   1. docs.uniswap.org/    (= developers.uniswap.org/ — ALIAS, see below)
#                            Unified Uniswap developer documentation covering:
#                            all protocol versions (v2, v3, v4, UniswapX,
#                            Unichain). For v4 specifically:
#                            - Concepts: architecture, hooks, dynamic fees,
#                              flash accounting, singleton PoolManager,
#                              native ETH support, custom accounting.
#                            - Guides: hook deployment, liquidity hooks,
#                              dynamic fee hooks, LP fee calculation.
#                            - Contracts reference: IPoolManager, IHooks,
#                              BaseHook, ProtocolFees, protocol-fee overview
#                              (TokenJar, Fee Adapters, Releasers → UNI burn).
#                            - Security: v4 audits (OpenZeppelin Jul 2024,
#                              Certora Jul 2024, Trail of Bits Sep 2024,
#                              Spearbit Sep 2024, ABDK Sep 2024 — core;
#                              OpenZeppelin/Spearbit/ABDK Sep 2024 — periphery);
#                              $15.5M bug bounty (Cantina, Nov 2024 launch);
#                              Hook security framework (Uniswap Foundation,
#                              public, self-directed).
#                            - Protocol fee: UNIfication governance proposal
#                              passed Dec 25, 2025 (125M+ votes vs 742
#                              dissenting); fee switch active on v2+v3 mainnet;
#                              v4 fee adapter (V4FeeAdapter) listed as "TBD"
#                              / work-in-progress in github.com/uniswap/
#                              protocol-fees — NOT yet active as of 2026-04-28.
#
# Aliasing detected (1 alias confirmed — DO NOT crawl separately):
#   developers.uniswap.org/ — fetched 2026-04-28; returns IDENTICAL HTML to
#   docs.uniswap.org/ (same Mintlify build, same nav, same "Uniswap
#   Documentation" heading, identical structure). Both hostnames resolve to
#   the same Mintlify deployment. Excluded from flat URL list; docs.uniswap.org
#   is canonical (CNAME target appears to be docs.uniswap.org per redirect).
#
# Out of scope for mirror layer (handle separately if needed):
#   - github.com/Uniswap/v4-core             (source code + audits dir — fetch
#     specific audit PDFs ad-hoc; not a docs format target)
#   - github.com/Uniswap/v4-periphery        (periphery source + audits dir)
#   - github.com/uniswapfoundation/security-framework  (hook security worksheet
#     repo; content already mirrored into docs.uniswap.org/developers/v4/security)
#   - github.com/uniswap/protocol-fees       (fee adapter contracts source)
#   - app.uniswap.org/                       (live DEX app; not docs)
#   - gov.uniswap.org/                       (governance forum; fetch ad-hoc
#     for specific governance proposals — UNIfication thread relevant for A7)
#   - blog.uniswap.org/                      (marketing/announcements; fetch
#     ad-hoc — v4 bug bounty launch post is relevant for A8)
#   - cantina.xyz/bounties/uniswap-v4        (bug bounty platform page; fetch
#     ad-hoc for A8/PV bounty terms)
#   - www.v4-by-example.org/                 (community tutorial site; not
#     first-party Uniswap docs)
#
# Path-prefix scoping for v4 content:
#   docs.uniswap.org/ hosts ALL protocol versions. To avoid crawling v1/v2/v3
#   content and UniswapX/Unichain content, scope to the v4 path prefixes:
#     /contracts/v4/    — core + periphery contract reference
#     /contracts/protocol-fee/  — protocol fee architecture (cross-version
#                                  but v4 fee adapter is here)
#     /sdk/v4/          — SDK reference (if present)
#   The developer-facing concepts and guides live under:
#     (docs served via developers.uniswap.org but at same canonical host)
#     /docs/protocols/v4/   — architecture, hooks concepts, guides
#   IMPORTANT: because both hostnames alias to the same deployment, ALL
#   v4 content lives under docs.uniswap.org/. Use TWO seed URLs with
#   distinct path prefixes to bound the BFS to v4-only content.
#   (One snapshot per seed = two snapshots total for v4.)
#
# Uniswap v4-specific structural notes for crawl agents:
#   - docs.uniswap.org uses Mintlify with client-side nav. Check for
#     /llms.txt or /sitemap.xml at the host root to seed BFS.
#   - The /contracts/v4/ subtree includes auto-generated Solidity interface
#     docs (IPoolManager, IHooks, ProtocolFees, etc.) — parameter-dense and
#     high-value for A2/A6/A8 claims. Likely 50+ pages; +max-pages=150.
#   - The /contracts/protocol-fee/ subtree covers protocol fee architecture
#     (TokenJar, Fee Adapters, Releasers / Firepit UNI burn). V4FeeAdapter
#     is "TBD" as of recon; content may be sparse or stub pages only.
#   - Dynamic fee docs at /contracts/v4/concepts/dynamic-fees — confirmed
#     accessible (content rendered correctly in recon fetch).
#
# === Frame coverage expectations (perpdex 9 angles × 5 patterns) ===
#
# IMPORTANT PREAMBLE: Uniswap v4 is NOT a perpdex entity. The 9×5 frame
# is designed for perpdex entities. Coverage below is provided for
# COMPLETENESS (to satisfy Phase 0 queue format) but most cells will be
# marked NOT-APPLICABLE with rationale. The applicable subset documents
# Uniswap v4 as a substrate that CAN host perpdex hooks — not as a perpdex
# itself. Frame is applied "substrate perspective" where each angle is
# re-read as "does v4 provide infrastructure for this angle?".
#
# Substrate-perspective frame predictions:
#
#   A1 launch       — Substrate perspective: Pool creation (initialize pool
#                     with PoolKey including hook address, fee tier, tick
#                     spacing) IS the "listing" mechanism for v4 pools.
#                     Permissionless — anyone can create a pool.
#                     For perpdex hooks specifically: the hook contract defines
#                     its own launch/listing logic. First-party v4 docs cover
#                     pool initialization mechanics well.
#                     Coverage (substrate angle): STRONG on pool creation
#                     mechanics. Perpdex-specific listing logic (how a perp
#                     hook decides to list a new market) — NOT-APPLICABLE at
#                     v4 protocol level (delegated entirely to hook developer).
#
#   A2 fee-model    — Substrate perspective: v4 has three fee layers:
#                     (1) LP swap fees (static tier 0–100% in 0.0001%
#                         increments, OR dynamic via hook beforeSwap return);
#                     (2) Protocol fees (set by governance via ProtocolFees
#                         contract; currently ~1/6 of swap fee on enabled
#                         pools; revenue → TokenJar → UNI burn via Firepit);
#                     (3) Hook fees (custom hook logic can extract additional
#                         fees via BeforeSwapDelta / AfterSwapDelta).
#                     UNIfication (Dec 25, 2025): activated protocol fees on
#                     v2+v3; v4 fee adapter TBD (V4FeeAdapter work-in-progress).
#                     Coverage: STRONG on fee architecture. V4 protocol fee
#                     activation timeline — PARTIAL (TBD, not yet active).
#
#   A3 referral     — Substrate perspective: v4 has no first-party referral
#                     or incentive program. Hooks COULD implement referral
#                     logic. No Uniswap v4 native referral program exists.
#                     Coverage: NOT-APPLICABLE at v4 protocol level — no
#                     first-party referral/incentive system.
#
#   A4 MM           — Substrate perspective: v4 supports JIT liquidity
#                     via hooks (beforeSwap hooks can inject single-block
#                     liquidity). No protocol-as-MM (no HLP/OmniVault
#                     equivalent in v4 itself). Hook developers can build
#                     custom MM logic. No formal MM program at v4 level.
#                     Coverage: PARTIAL — JIT/liquidity hook mechanics
#                     documented. Protocol-MM model — NOT-APPLICABLE.
#
#   A5 spot-LP      — Substrate perspective: v4 IS fundamentally a spot-LP
#                     DEX substrate. LP mechanics (concentrated liquidity
#                     inherited from v3; dynamic fees; custom accounting
#                     hooks for LP fee redistribution) are CORE to v4.
#                     This is the primary use case — most A5 cells APPLICABLE.
#                     Coverage: STRONG on LP mechanics, fee accrual,
#                     position management. LVR/IL tooling — PARTIAL
#                     (hooks can implement mitigation but first-party
#                     guidance is general). LP exit UX — PARTIAL.
#
#   A6 risk         — Substrate perspective: v4 has no insurance fund,
#                     no ADL, no liquidation engine — these are perpdex
#                     concerns delegated to hook contracts. Hook security
#                     framework (developers.uniswap.org/docs/protocols/v4/
#                     security) provides a risk-scoring rubric for hook
#                     developers. Pool-level risk: reentrancy via hooks,
#                     flash accounting delta manipulation, external dependency
#                     failures, TVL scaling.
#                     Coverage: PARTIAL (hook risk framework well-documented;
#                     perpdex-specific risk concepts like IF/ADL —
#                     NOT-APPLICABLE at v4 protocol level).
#
#   A7 governance   — Substrate perspective: UNI token governance controls
#                     protocol fee settings (ProtocolFeeController), fee
#                     adapter configuration, and Releaser selection.
#                     UNIfication proposal (Dec 2025): governance process
#                     fully documented (Snapshot + on-chain vote, 2-day
#                     timelock). Parameter change lead time = Snapshot
#                     (off-chain) → on-chain vote → 2-day timelock → execution.
#                     Core contracts (PoolManager, TokenJar) are IMMUTABLE.
#                     Coverage: STRONG on governance mechanics for protocol
#                     fee parameters. Pool-level parameter authority
#                     (tick spacing, fee tier) — set at pool creation,
#                     immutable after. Hook-level parameters — hook-specific,
#                     not v4 protocol concern.
#
#   A8 security     — Substrate perspective: v4 core has 9 independent
#                     audits (OpenZeppelin × 2 for core + periphery,
#                     Certora, Trail of Bits, Spearbit × 2, ABDK × 2;
#                     July–September 2024); $2.35M security competition
#                     (500+ researchers, no critical vulns found); $15.5M
#                     bug bounty on Cantina (launched Nov 2024 — largest
#                     in DeFi history at launch). Hook security is hook-
#                     developer's responsibility, with guidance from
#                     Uniswap Foundation security framework.
#                     Coverage: STRONG on v4 core audit history + bug
#                     bounty. Hook-level security — framework documented
#                     but individual hook audits are third-party.
#
#   A9 regulatory   — Substrate perspective: v4 is permissionless, non-
#                     custodial DEX protocol. No jurisdiction block list
#                     in first-party docs. UNIfication included Wyoming
#                     DUNA (Decentralized Unincorporated Nonprofit
#                     Association) structure for Uniswap Labs alignment.
#                     Interface fees eliminated by Labs under UNIfication.
#                     Unichain (Uniswap's L2) is separate entity.
#                     Regulatory classification for v4 as DEX substrate —
#                     NOT documented in first-party docs.
#                     Coverage: PARTIAL on governance/legal structure
#                     (UNIfication context). Regulatory classification
#                     of v4 itself — GAP.
#
# === Predicted gaps to validate post-ingest ===
#
# G(uniswap-v4)-1 — V4 protocol fee activation status and timeline (A2 × P-II):
#   UNIfication passed Dec 25, 2025 activating fees on v2+v3 mainnet pools.
#   V4FeeAdapter listed as "TBD" in github.com/uniswap/protocol-fees (as of
#   recon 2026-04-28). Whether v4 protocol fees have been activated, and
#   what the governance timeline is, will only be confirmed post-ingest.
#   Flag: high priority, time-sensitive gap.
#
# G(uniswap-v4)-2 — Protocol fee rate on v4 pools (A2 × P-I quality metric):
#   UNIfication sets initial protocol fee at ~1/6 of swap fee. Whether this
#   applies to v4 pools (once V4FeeAdapter activates) or if a different rate
#   is proposed — not documented. On-chain query or secondary governance
#   source required post-ingest.
#
# G(uniswap-v4)-3 — Perpdex hook inventory on v4 (A1 × P-I quality metric):
#   Which perpetual futures hooks are actually deployed on Uniswap v4 mainnet?
#   First-party docs cover hook mechanism, not the ecosystem of deployed
#   hooks. This is the core "transitive perpdex" question: without an
#   inventory of deployed perpdex hooks, the v4 substrate has no direct
#   perpdex-frame content. Requires secondary research (DeFiLlama, Dune
#   analytics, or hook registry) — out of scope for this mirror crawl.
#   If no perpdex hooks are found, Uniswap v4 has zero direct perpdex
#   frame coverage (all cells remain not-applicable or gap).
#
# G(uniswap-v4)-4 — Bug bounty scope: hooks vs core (A8 × P-V SLA-contracting):
#   The $15.5M Cantina bounty covers v4 core and periphery contracts.
#   Third-party hook contracts are explicitly OUT OF SCOPE. Hook developers
#   must run their own bounties (Uniswap Foundation security framework
#   recommends this for high-TVL hooks). The boundary between "v4 core bug"
#   and "hook bug" in terms of bounty eligibility needs first-party
#   clarification. Cantina bounty page (out of scope for mirror) holds the
#   definitive answer — fetch ad-hoc for A8/PV.
#
# G(uniswap-v4)-5 — Dynamic fee governance: who can change per-pool fees (A7 × P-I):
#   Dynamic fees are set at pool creation and managed by the hook contract
#   via updateDynamicLPFee or beforeSwap return. Static fees are set at
#   pool creation and IMMUTABLE. Protocol fees (separate from LP fees) are
#   governance-controlled via ProtocolFeeController. The authority structure
#   for "who governs what fee layer" needs explicit documentation —
#   partially covered in recon but needs post-ingest validation.
#
# G(uniswap-v4)-6 — Regulatory classification / jurisdiction (A9 × P-I–V):
#   No first-party legal opinion or blocked-jurisdiction list found. Uniswap
#   Labs has historically faced SEC scrutiny; UNIfication included Wyoming
#   DUNA structure. Formal regulatory classification of v4 as DEX substrate
#   (non-custodial, permissionless) — GAP in first-party docs.
#
# G(uniswap-v4)-7 — Subtype schema gap: `dex-substrate` entity type (meta):
#   No existing AGENTS.md entity subtype covers Uniswap v4 accurately.
#   `perpdex` is wrong (v4 is not a perpdex). `network` is wrong (v4 is
#   not a blockchain). `actor` and `token` don't apply. A new `dex-substrate`
#   subtype is needed. This is a Phase 2 schema extension, not a content
#   gap. Must be raised in open-questions.md before the entity page is
#   created.

https://docs.uniswap.org/contracts/v4/
https://docs.uniswap.org/contracts/protocol-fee/
